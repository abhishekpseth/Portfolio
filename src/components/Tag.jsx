// Small pill used for tech-stack chips.
const Tag = ({ children }) => (
  <span className="rounded-full border border-line bg-surface2 px-3 py-1 font-mono text-xs text-muted">
    {children}
  </span>
);

export default Tag;
